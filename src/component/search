

const Search = ({seaechs , titleSearch}) => {
   
    const handel = (event) =>{
      
      seaechs(event);
    }
    return(
 <>
  <label htmlFor="search">Search</label>
    <input onChange={handel} type="text" id="search" value={titleSearch} />
 </>
 
    )
  }
  export default Search;
